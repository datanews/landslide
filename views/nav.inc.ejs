
<nav class="main-navbar navbar navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#nav-bar-collapsable" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      <a class="navbar-brand" href="/" title="ElectionLand reporting home">
        <img alt="ElectionLand logo" src="/media/Electionland-logo-edit.svg">
      </a>

      <p class="navbar-text"><%= __('siteTitle') %></p>
    </div>

    <div class="collapse navbar-collapse" id="nav-bar-collapsable">
      <ul class="navbar-nav navbar-right">
        <!--
        <% if (getLocale() === 'en') { %>
          <li><p class="navbar-text">
            <a class="alt-link" href="<%= path %>?lang=es" title="español">español</a>
          </p></li>
        <% } else { %>
          <li><p class="navbar-text">
            <a class="alt-link" href="<%= path %>?lang=en" title="english">english</a>
          </p></li>
        <% } %>
        -->

        <% if (user) { %>
          <li><p class="navbar-text">
            <%= user.displayName %> &nbsp;
            <img class="user-image" src="<%= user.imageURL %>"> &nbsp;
            <a href="/logout" title="<%= __('logout') %>"><%= __('logout') %> <i class="fa fa-sign-out"></i></a>
          </p></li>
        <% } else { %>
          <li><p class="navbar-text"><a href="/login" title="<%= __('login') %>"><%= __('login') %> <i class="fa fa-sign-in"></i></a></p></li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
